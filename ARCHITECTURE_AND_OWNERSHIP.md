# معمارية رَكيزة وملكية العميل وقابلية النقل

## نطاق الوثيقة

تصف هذه الوثيقة النسخة الحالية من رَكيزة، وهي منصة إدارية داخلية تعمل بواجهة React وTypeScript، وخادم Express مع tRPC، ومخطط Drizzle لقاعدة MySQL/TiDB. تحفظ الملفات الثنائية في التخزين الكائني عبر خدمة التخزين، ولا تُحفظ المفاتيح السرية أو محتوى OTP داخل المستودع.

## خريطة المكونات

| المكوّن | المسؤولية | قابلية النقل |
|---|---|---|
| واجهة React وTailwind | صفحات RTL، الهيكل، المهام، المراسلات، التقارير، الموظفون، المساعدون | قابلة للنقل إلى استضافة Node/Vite مناسبة |
| خادم Express وtRPC | المصادقة، الصلاحيات، العزل، قواعد العمل، الإشعارات | يعتمد على Node وبيئة متغيرات سرية |
| Drizzle وMySQL/TiDB | الوحدات، الملفات، المهام، الأرشيف، المراسلات، التدقيق | يمكن نقل المخطط إلى PostgreSQL بعد مواءمة أنواع MySQL والهجرات |
| التخزين الكائني | المرفقات والملفات المرجعية | يحتاج محولاً لخدمة S3 أو Azure Blob أو تخزين داخلي |
| Brevo | OTP والبريد التنبيهي الحالي | يمكن استبداله بموفر مؤسسي أو Microsoft Graph |
| Heartbeat | التذكيرات والمزامنة والتصعيدات الدورية | يحتاج مجدولاً مؤسسياً أو عامل تشغيل دائم في بيئة بديلة |

## العزل والصلاحيات

يُطبق العزل في الخادم قبل إرجاع البيانات، لا في الواجهة فقط. يستخدم النظام نطاق الصلاحية والملف والوحدة والقاضي المشرف لتحديد ما يمكن عرضه، وتُسجل عمليات النقل والتكليف والإلغاء والاسترجاع في سجل التدقيق.

## التصدير والنقل

تُحفظ الشيفرة في مستودع GitHub خاص، وتُدار الأسرار خارج المستودع. نقل قاعدة البيانات يتطلب تصديراً منظماً للبيانات مع اختبار مخطط الوجهة، ثم نقل ملفات التخزين ومراجعة الروابط والصلاحيات. لا يُعد وجود مستودع GitHub نسخة من قاعدة البيانات أو المرفقات.

## الملكية والمسؤوليات

يُفترض أن تكون الشيفرة والمخطط وملفات التوثيق وسير العمل والنماذج المخصصة ملكاً للجهة وفق عقدها واتفاقها مع منفذ التطوير. يجب تثبيت ذلك بعقد أو محضر تسليم رسمي، لأن هذه الوثيقة لا تنشئ حقاً قانونياً بذاتها. يجب أن تستلم الجهة نسخة من الشيفرة، مخطط قاعدة البيانات، قائمة الأسرار بأسماء المتغيرات فقط، إجراءات الاستعادة، ودليل تشغيل الوظائف الدورية.

## حدود الاعتماد الخارجي

يتطلب تشغيل Teams وMicrosoft Graph وEntra ونسخ احتياطي خارجي وبيئة On-Premise اعتماداً وبيانات من الجهة. كما يتطلب اختبار الاختراق ومراجعة حماية البيانات فريقاً مؤسسياً مختصاً؛ الاختبارات المحلية في المستودع لا تُعد بديلاً عنهما.
