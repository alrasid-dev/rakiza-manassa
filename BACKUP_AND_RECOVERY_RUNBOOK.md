# دليل النسخ الاحتياطي والاستعادة في رَكيزة

## الحالة التشغيلية

هذا الدليل هو **تصميم جاهز للتفعيل**. لا تُفعّل وجهة خارجية قبل أن يعتمد مالك المنصة الوجهة وحساب الخدمة ومفتاح التشفير وآلية الاحتفاظ. إبقاء المسار غير مفعّل حالياً يحمي بيانات المحكمة من الإرسال العرضي إلى تخزين غير معتمد.

## النطاق والجدولة

تُنفذ النسخة الأسبوعية مساء الجمعة بعد انتهاء دورة التشغيل، وتُحفظ نسخة قاعدة البيانات وبيانات الإعدادات وسجل التدقيق ومراجع الملفات المخزنة. لا تُنسخ أسرار البيئة أو رموز الجلسات أو مفاتيح Brevo إلى ملف النسخة. تظل المرفقات في التخزين الكائني وفق مراجعها، أو تُنسخ إلى حاوية خارجية مشفرة إذا اعتمدت الجهة ذلك صراحة.

## التشفير والاحتفاظ

يجب أن يكون التشفير أثناء النقل وعند التخزين، ويُدار مفتاح التشفير خارج قاعدة البيانات وخارج المستودع. تحفظ النسخ وفق ثلاث طبقات: آخر نسخة ناجحة، ونسخ الأسابيع السابقة ضمن مدة الاحتفاظ المعتمدة، ونسخة اختبار استعادة منفصلة لا تحتوي على بيانات تشغيلية مكشوفة. لا يُسجل المفتاح أو كلمة المرور في سجل الحركة أو رسائل البريد.

## سجل التدقيق والتحقق

لكل عملية نسخة يسجل النظام تاريخ البدء والانتهاء، النتيجة، حجم النسخة، بصمة تحقق، وجهة التخزين، مدة الاحتفاظ، ومعرف مهمة الجدولة. لا تُعتبر النسخة ناجحة إلا بعد التحقق من سلامة الأرشيف ووجود جداول أساسية مثل المستخدمين والملفات والمهام وسجل الحركة. تُرسل نتيجة النجاح أو الفشل إلى القيادة، ويُمنع تكرار التنبيه لنفس دورة النسخة.

## الاستعادة

تبدأ الاستعادة بفتح طلب قيادة موثق، ثم عزل نسخة الاستعادة عن الإنتاج، والتحقق من بصمة النسخة، وتجربة القراءة في بيئة منفصلة. بعد اعتماد النتيجة، تُنفذ الاستعادة على نافذة صيانة، ويُراجع سجل المستخدمين والصلاحيات والعزل الإداري قبل إعادة الإتاحة. يجب تسجيل المنفذ والوقت ونقطة الاستعادة والنتيجة وأي جداول لم تُستعد.

## متطلبات التفعيل

| المتطلب | الحالة | المسؤول عن توفيره |
|---|---|---|
| وجهة خارجية معتمدة | معلّق | مالك المنصة/الجهة |
| حساب خدمة محدود الصلاحيات | معلّق | مسؤول البنية المؤسسية |
| مفتاح تشفير خارج بيئة التطبيق | معلّق | مسؤول الأمن أو البنية |
| مدة الاحتفاظ النهائية | معلّق | مالك المنصة وفق سياسة الجهة |
| اختبار استعادة مقبول | يطبق بعد التفعيل | مالك المنصة مع مسؤول التشغيل |

> لا تُعد النسخة الحالية مفعلة للنسخ الخارجي؛ التصميم محفوظ وجاهز بعد توفير عناصر الجدول أعلاه. لا توجد بيانات اختبار أو نسخ وهمية مضافة إلى قاعدة البيانات.
